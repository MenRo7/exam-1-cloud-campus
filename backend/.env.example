# Backend Environment Variables Example
# Copier ce fichier en .env et remplir avec vos vraies valeurs

# Base de données MongoDB
MONGO_URI=mongodb://mongo:27017/mydatabase
# Pour production (Railway/MongoDB Atlas):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/ecommerce?retryWrites=true&w=majority

# JWT Secret (générer avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=votre_secret_super_securise_256_bits_minimum

# Environnement
NODE_ENV=development
# Pour production: NODE_ENV=production

# Port
PORT=5000

# CORS - Origines autorisées (séparées par des virgules)
ALLOWED_ORIGINS=http://localhost:3000,https://votre-app.vercel.app,https://*.vercel.app

# Email (pour service notifications)
EMAIL_USER=votre.email@gmail.com
EMAIL_APPLICATION_PASSWORD=votre_app_password_gmail

# Ports des services
GATEWAY_PORT=8000
NOTIFI_PORT=4002
